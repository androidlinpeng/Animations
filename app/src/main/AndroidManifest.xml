<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.msgcopy.application">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AnimationActivity" />
        <activity android:name=".CustomCircleActivity" />
        <activity android:name=".PaletteViewActivity" />
        <activity android:name=".SemicircleViewActivity" />
        <activity android:name=".GestureDetectorActivity" />
        <activity
            android:name=".ProgressViewActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".BlurryActivity" />
        <activity android:name=".FrameAnimationActivity" />
        <activity android:name=".LoadAnimActivity"></activity>
    </application>

</manifest>